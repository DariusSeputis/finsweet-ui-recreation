import styled from 'styled-components';

export const StyledButtonPrimary = styled.div`
  cursor: pointer;

  color: var(--white-color);
  background-color: var(--primary-color);

  display: flex;
  flex-direction: row;
  align-items: flex-start;
  padding: 16px 40px;

  border-radius: 12px;

  width: fit-content;
`;
export const StyledButtonBlack = styled.div`
  cursor: pointer;

  color: var(--white-color);
  background-color: var(--black-color);

  display: flex;
  flex-direction: row;
  align-items: flex-start;
  padding: 16px 40px;

  border-radius: 12px;

  width: fit-content;
`;
